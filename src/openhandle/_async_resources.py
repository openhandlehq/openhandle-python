# Generated by scripts/generate.py. Do not edit.

from __future__ import annotations

from collections.abc import AsyncIterator
from datetime import datetime
from typing import Any, Literal

from openhandle import models
from openhandle._references import resolve_selector
from openhandle._response import AsyncPage, Response
from openhandle._transport import AsyncTransport
from openhandle._types import Freshness


class AsyncInstagramCommentLikersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, media_id: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfileSummary]:
        """List comment likers

        List comment likers from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.comment("910100000001").likers.list(media_id="910100000001", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-comment-likers-list
        """
        params: dict[str, Any] = {"media_id": media_id, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/comments/{identifier}/likers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/comments/{identifier}/likers", self._bindings, params, timeout, max_retries)

    async def items(self, *, media_id: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfileSummary]:
        """Iterate items lazily across pages of instagram.comment.likers.list, one request per page."""
        page: AsyncPage[models.InstagramProfileSummary] | None = await self.list(media_id=media_id, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramCommentResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.likers = AsyncInstagramCommentLikersResource(transport, bindings)


class AsyncInstagramHashtagPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List hashtag posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.hashtag("syntheticvolume").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-hashtag-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/hashtags/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/hashtags/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.hashtag.posts.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(sort=sort, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramHashtagReelsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List hashtag reels

        Return a cursor-paginated collection of Instagram reels for a hashtag. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.hashtag("910100000001").reels.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-hashtag-reels-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/hashtags/{identifier}/reels", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/hashtags/{identifier}/reels", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.hashtag.reels.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramHashtagResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncInstagramHashtagPostsResource(transport, bindings)
        self.reels = AsyncInstagramHashtagReelsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramHashtag]:
        """Get a hashtag

        Return one public hashtag by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.hashtag("syntheticvolume").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-hashtag-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/hashtags/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramHighlightResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramHighlight]:
        """Get a highlight

        Get a highlight from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.highlight("910100000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-highlight-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/highlights/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramLocationPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List location posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.location("950000000002").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-location-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/locations/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/locations/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.location.posts.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(sort=sort, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramLocationResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncInstagramLocationPostsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramLocation]:
        """Get a location

        Return one public location by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.location("950000000002").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-location-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/locations/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramMusicStreamResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramMusicStream]:
        """List music stream pages

        List music stream pages from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.music("910100000001").stream.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-music-stream-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/music/{identifier}/stream", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/music/{identifier}/stream", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramMusicStream]:
        """Iterate items lazily across pages of instagram.music.stream.list, one request per page."""
        page: AsyncPage[models.InstagramMusicStream] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramMusicResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.stream = AsyncInstagramMusicStreamResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramMusic]:
        """Get a music

        Return one public music by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.music("950000000003").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-music-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/music/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostClipMetadataResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramClipMetadata]:
        """Get clip metadata

        Get clip metadata from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").clip_metadata.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-clip-metadata-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/clip-metadata", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostCommentRepliesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramComment]:
        """List comment replies

        Return a stable page of replies for one comment. Use find_test_data to discover compatible post and comment IDs. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").comment("910200000001").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-comment-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramComment]:
        """Iterate items lazily across pages of instagram.post.comment.replies.list, one request per page."""
        page: AsyncPage[models.InstagramComment] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramPostCommentResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.replies = AsyncInstagramPostCommentRepliesResource(transport, bindings)


class AsyncInstagramPostCommentMetadataResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramCommentMetadata]:
        """Get comment metadata

        Get comment metadata from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").comment_metadata.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-comment-metadata-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/comment-metadata", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostCommentModerationResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, comment: str, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramCommentModeration]:
        """Check comment moderation

        Check comment moderation from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").comment_moderation.get(comment="synthetic comment", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-comment-moderation-get
        """
        params: dict[str, Any] = {"comment": comment, "freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/comment-moderation", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostCommentsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramComment]:
        """List post comments

        List post comments by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-comments-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramComment]:
        """Iterate items lazily across pages of instagram.post.comments.list, one request per page."""
        page: AsyncPage[models.InstagramComment] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramPostInsightsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramPostInsight]:
        """Get post insights

        Get post insights from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").insights.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-insights-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/insights", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostLikersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """List post likers

        Return public profiles that liked an Instagram post. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").likers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-likers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/likers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/likers", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.post.likers.list, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramPostOEmbedResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramOEmbed]:
        """Get post oEmbed data

        Get post oEmbed data from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").oembed.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-oembed-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/oembed", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostTaggedUsersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramUserTag]:
        """List tagged users

        Return public profiles tagged in an Instagram post. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").tagged_users.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-tagged-users-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/tagged-users", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/tagged-users", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramUserTag]:
        """Iterate items lazily across pages of instagram.post.taggedUsers.list, one request per page."""
        page: AsyncPage[models.InstagramUserTag] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramPostTemplateResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramTemplate]:
        """Get post template

        Get post template from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").template.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-template-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}/template", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramPostResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.clip_metadata = AsyncInstagramPostClipMetadataResource(transport, bindings)
        self.comment_metadata = AsyncInstagramPostCommentMetadataResource(transport, bindings)
        self.comment_moderation = AsyncInstagramPostCommentModerationResource(transport, bindings)
        self.comments = AsyncInstagramPostCommentsResource(transport, bindings)
        self.insights = AsyncInstagramPostInsightsResource(transport, bindings)
        self.likers = AsyncInstagramPostLikersResource(transport, bindings)
        self.oembed = AsyncInstagramPostOEmbedResource(transport, bindings)
        self.tagged_users = AsyncInstagramPostTaggedUsersResource(transport, bindings)
        self.template = AsyncInstagramPostTemplateResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramPostCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="comment")
        return AsyncInstagramPostCommentResource(self._transport, {**self._bindings, "comment_id": identifier})

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramPost]:
        """Get a post

        Get a post by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.post("910100000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramProfileAboutResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramProfileAbout]:
        """Get profile about information

        Get profile about information from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").about.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-about-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/about", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramProfileBusinessRecommendationsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfileSummary]:
        """List business recommendations

        List business recommendations from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").business_recommendations.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-business-recommendations-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/business-recommendations", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/business-recommendations", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfileSummary]:
        """Iterate items lazily across pages of instagram.profile.businessRecommendations.list, one request per page."""
        page: AsyncPage[models.InstagramProfileSummary] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileFollowersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """List profile followers

        Return a cursor-paginated collection of Instagram profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").followers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-followers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/followers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/followers", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.followers.list, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()

    async def search(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """Search profile followers

        Search within a public profile's followers collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").followers.search(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-followers-search
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/followers/search", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/followers/search", self._bindings, params, timeout, max_retries)

    async def search_items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.followers.search, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.search(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileFollowingResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """List profile following

        Return a cursor-paginated collection of Instagram profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").following.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-following-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/following", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/following", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.following.list, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()

    async def search(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """Search profile following

        Search within a public profile's following collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").following.search(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-following-search
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/following/search", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/following/search", self._bindings, params, timeout, max_retries)

    async def search_items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.following.search, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.search(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileHighlightsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramHighlight]:
        """List profile highlights

        List profile highlights from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").highlights.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-highlights-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/highlights", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/highlights", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramHighlight]:
        """Iterate items lazily across pages of instagram.profile.highlights.list, one request per page."""
        page: AsyncPage[models.InstagramHighlight] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfilePinnedPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List profile pinned posts

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").pinned_posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-pinned-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/pinned-posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/pinned-posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.pinnedPosts.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfilePostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List profile posts

        Return a stable page of posts for one public profile. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor, "since": since}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.posts.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, since=since, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileReelsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List profile reels

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").reels.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-reels-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/reels", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/reels", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.reels.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileRepostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List profile reposts

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").reposts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-reposts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/reposts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/reposts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.reposts.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileStoriesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramStory]:
        """List profile stories

        List profile stories from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").stories.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-stories-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/stories", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/stories", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramStory]:
        """Iterate items lazily across pages of instagram.profile.stories.list, one request per page."""
        page: AsyncPage[models.InstagramStory] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileSuggestedResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """List profile suggested

        Return a cursor-paginated collection of Instagram profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").suggested.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-suggested-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/suggested", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/suggested", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.suggested.list, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileTaggedPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List profile tagged posts

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").tagged_posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-tagged-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/tagged-posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/tagged-posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.taggedPosts.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileVideosResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """List profile videos

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").videos.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-videos-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}/videos", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/videos", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.videos.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramProfileResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.about = AsyncInstagramProfileAboutResource(transport, bindings)
        self.business_recommendations = AsyncInstagramProfileBusinessRecommendationsResource(transport, bindings)
        self.followers = AsyncInstagramProfileFollowersResource(transport, bindings)
        self.following = AsyncInstagramProfileFollowingResource(transport, bindings)
        self.highlights = AsyncInstagramProfileHighlightsResource(transport, bindings)
        self.pinned_posts = AsyncInstagramProfilePinnedPostsResource(transport, bindings)
        self.posts = AsyncInstagramProfilePostsResource(transport, bindings)
        self.reels = AsyncInstagramProfileReelsResource(transport, bindings)
        self.reposts = AsyncInstagramProfileRepostsResource(transport, bindings)
        self.stories = AsyncInstagramProfileStoriesResource(transport, bindings)
        self.suggested = AsyncInstagramProfileSuggestedResource(transport, bindings)
        self.tagged_posts = AsyncInstagramProfileTaggedPostsResource(transport, bindings)
        self.videos = AsyncInstagramProfileVideosResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramProfile]:
        """Get a profile

        Return one public profile. Use @username or a platform ID as the identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.profile("@northstar_forge_test").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramSearchHashtagsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramHashtag]:
        """Search Instagram hashtags

        Search public Instagram hashtags with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.search.hashtags.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-hashtags-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/search/hashtags", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/hashtags", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramHashtag]:
        """Iterate items lazily across pages of instagram.search.hashtags.list, one request per page."""
        page: AsyncPage[models.InstagramHashtag] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramSearchMusicResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramMusic]:
        """Search Instagram music

        Search public Instagram music with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.search.music.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-music-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/search/music", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/music", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramMusic]:
        """Iterate items lazily across pages of instagram.search.music.list, one request per page."""
        page: AsyncPage[models.InstagramMusic] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramSearchPlacesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramLocation]:
        """Search Instagram places

        Search public Instagram places with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.search.places.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-places-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/search/places", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/places", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramLocation]:
        """Iterate items lazily across pages of instagram.search.places.list, one request per page."""
        page: AsyncPage[models.InstagramLocation] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramSearchProfilesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramProfile]:
        """Search Instagram profiles

        Search public Instagram profiles with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.search.profiles.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-profiles-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/search/profiles", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/profiles", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.search.profiles.list, one request per page."""
        page: AsyncPage[models.InstagramProfile] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramSearchReelsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramPost]:
        """Search Instagram reels

        Search public Instagram reels with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.search.reels.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-reels-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/search/reels", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/reels", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.search.reels.list, one request per page."""
        page: AsyncPage[models.InstagramPost] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramSearchTopResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.InstagramSearchResult]:
        """Search all Instagram results

        Search all Instagram results from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.search.top.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-top-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/instagram/search/top", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/top", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.InstagramSearchResult]:
        """Iterate items lazily across pages of instagram.search.top.list, one request per page."""
        page: AsyncPage[models.InstagramSearchResult] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncInstagramSearchResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.hashtags = AsyncInstagramSearchHashtagsResource(transport, bindings)
        self.music = AsyncInstagramSearchMusicResource(transport, bindings)
        self.places = AsyncInstagramSearchPlacesResource(transport, bindings)
        self.profiles = AsyncInstagramSearchProfilesResource(transport, bindings)
        self.reels = AsyncInstagramSearchReelsResource(transport, bindings)
        self.top = AsyncInstagramSearchTopResource(transport, bindings)


class AsyncInstagramStoryResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramStory]:
        """Get a story

        Get a story from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.instagram.story("910100000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-story-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/instagram/stories/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncInstagramResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.search = AsyncInstagramSearchResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="comment")
        return AsyncInstagramCommentResource(self._transport, {**self._bindings, "identifier": identifier})

    def hashtag(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramHashtagResource:
        """Select the hashtag resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="hashtag")
        return AsyncInstagramHashtagResource(self._transport, {**self._bindings, "identifier": identifier})

    def highlight(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramHighlightResource:
        """Select the highlight resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="highlight")
        return AsyncInstagramHighlightResource(self._transport, {**self._bindings, "identifier": identifier})

    def location(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramLocationResource:
        """Select the location resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="location")
        return AsyncInstagramLocationResource(self._transport, {**self._bindings, "identifier": identifier})

    def music(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramMusicResource:
        """Select the music resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="music")
        return AsyncInstagramMusicResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="post")
        return AsyncInstagramPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> AsyncInstagramProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="instagram", resource="profile")
        return AsyncInstagramProfileResource(self._transport, {**self._bindings, "identifier": identifier})

    def story(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncInstagramStoryResource:
        """Select the story resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="story")
        return AsyncInstagramStoryResource(self._transport, {**self._bindings, "identifier": identifier})


class AsyncRedditDomainPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditPost]:
        """List domain posts

        List domain posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.domain("example.com").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-domain-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/domains/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/domains/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.domain.posts.list, one request per page."""
        page: AsyncPage[models.RedditPost] | None = await self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditDomainResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncRedditDomainPostsResource(transport, bindings)


class AsyncRedditPostCommentsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["confidence", "top", "new", "controversial", "old", "qa"] | None = None, limit: int | None = None, depth: int | None = None, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[list[models.RedditComment]]:
        """List post comments

        List post comments from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.post("synthetic").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-post-comments-list
        """
        params: dict[str, Any] = {"sort": sort, "limit": limit, "depth": depth, "freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditPostDuplicatesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditPost]:
        """List duplicate posts

        List duplicate posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.post("synthetic").duplicates.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-post-duplicates-list
        """
        params: dict[str, Any] = {"limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/posts/{identifier}/duplicates", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/posts/{identifier}/duplicates", self._bindings, params, timeout, max_retries)

    async def items(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.post.duplicates.list, one request per page."""
        page: AsyncPage[models.RedditPost] | None = await self.list(limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditPostResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = AsyncRedditPostCommentsResource(transport, bindings)
        self.duplicates = AsyncRedditPostDuplicatesResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditPost]:
        """Get a post

        Get a post from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.post("synthetic").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditProfileCommentsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditComment]:
        """List profile comments

        List profile comments from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.profile("@synthetic_reddit").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-comments-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/profiles/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/profiles/{identifier}/comments", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditComment]:
        """Iterate items lazily across pages of reddit.profile.comments.list, one request per page."""
        page: AsyncPage[models.RedditComment] | None = await self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditProfileModeratedResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[list[models.RedditSubreddit]]:
        """List moderated subreddits

        List moderated subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.profile("@synthetic_reddit").moderated.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-moderated-list
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/profiles/{identifier}/moderated", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditProfilePostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditPost]:
        """List profile posts

        List profile posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.profile("@synthetic_reddit").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.profile.posts.list, one request per page."""
        page: AsyncPage[models.RedditPost] | None = await self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditProfileTrophiesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[list[models.RedditTrophy]]:
        """List profile trophies

        List profile trophies from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.profile("@synthetic_reddit").trophies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-trophies-list
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/profiles/{identifier}/trophies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditProfileResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = AsyncRedditProfileCommentsResource(transport, bindings)
        self.moderated = AsyncRedditProfileModeratedResource(transport, bindings)
        self.posts = AsyncRedditProfilePostsResource(transport, bindings)
        self.trophies = AsyncRedditProfileTrophiesResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditProfile]:
        """Get a profile

        Return one public Reddit profile. Use @username or a t2_ profile ID. An ID resolves only after the profile was read by username once. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.profile("@synthetic_reddit").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditSearchPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, sort: Literal["relevance", "hot", "top", "new", "comments"] | None = None, subreddit: str | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditPost]:
        """Search posts

        Search posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.search.posts.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-search-posts-list
        """
        params: dict[str, Any] = {"q": q, "sort": sort, "subreddit": subreddit, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/search/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/search/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, sort: Literal["relevance", "hot", "top", "new", "comments"] | None = None, subreddit: str | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.search.posts.list, one request per page."""
        page: AsyncPage[models.RedditPost] | None = await self.list(q=q, sort=sort, subreddit=subreddit, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditSearchProfilesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditProfile]:
        """Search profiles

        Search profiles from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.search.profiles.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-search-profiles-list
        """
        params: dict[str, Any] = {"q": q, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/search/profiles", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/search/profiles", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditProfile]:
        """Iterate items lazily across pages of reddit.search.profiles.list, one request per page."""
        page: AsyncPage[models.RedditProfile] | None = await self.list(q=q, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditSearchSubredditsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditSubreddit]:
        """Search subreddits

        Search subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.search.subreddits.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-search-subreddits-list
        """
        params: dict[str, Any] = {"q": q, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/search/subreddits", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/search/subreddits", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.search.subreddits.list, one request per page."""
        page: AsyncPage[models.RedditSubreddit] | None = await self.list(q=q, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditSearchResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncRedditSearchPostsResource(transport, bindings)
        self.profiles = AsyncRedditSearchProfilesResource(transport, bindings)
        self.subreddits = AsyncRedditSearchSubredditsResource(transport, bindings)


class AsyncRedditSubredditPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditPost]:
        """List subreddit posts

        List subreddit posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddit("synthetic").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/subreddits/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.subreddit.posts.list, one request per page."""
        page: AsyncPage[models.RedditPost] | None = await self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditSubredditRulesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[list[models.RedditRule]]:
        """List subreddit rules

        List subreddit rules from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddit("synthetic").rules.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-rules-list
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/subreddits/{identifier}/rules", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditSubredditWikiPageResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditWikiPage]:
        """Get a wiki page

        Get a wiki page from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddit("synthetic").wiki_page("index").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-wiki-page-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/subreddits/{identifier}/wiki-pages/{page}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditSubredditWikiPagesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[list[models.RedditWikiPage]]:
        """List wiki pages

        List wiki pages from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddit("synthetic").wiki_pages.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-wiki-list
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/subreddits/{identifier}/wiki-pages", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditSubredditResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncRedditSubredditPostsResource(transport, bindings)
        self.rules = AsyncRedditSubredditRulesResource(transport, bindings)
        self.wiki_pages = AsyncRedditSubredditWikiPagesResource(transport, bindings)

    def wiki_page(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncRedditSubredditWikiPageResource:
        """Select the wikiPage resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="wikiPage")
        return AsyncRedditSubredditWikiPageResource(self._transport, {**self._bindings, "page": identifier})

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditSubreddit]:
        """Get a subreddit

        Get a subreddit from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddit("synthetic").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/reddit/subreddits/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncRedditSubredditsNewResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditSubreddit]:
        """List new subreddits

        List new subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddits.new.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddits-new-list
        """
        params: dict[str, Any] = {"limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/subreddits/new", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/new", self._bindings, params, timeout, max_retries)

    async def items(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.subreddits.new.list, one request per page."""
        page: AsyncPage[models.RedditSubreddit] | None = await self.list(limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditSubredditsPopularResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditSubreddit]:
        """List popular subreddits

        List popular subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.subreddits.popular.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddits-popular-list
        """
        params: dict[str, Any] = {"limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/subreddits/popular", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/popular", self._bindings, params, timeout, max_retries)

    async def items(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.subreddits.popular.list, one request per page."""
        page: AsyncPage[models.RedditSubreddit] | None = await self.list(limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditSubredditsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.new = AsyncRedditSubredditsNewResource(transport, bindings)
        self.popular = AsyncRedditSubredditsPopularResource(transport, bindings)


class AsyncRedditTrendingPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, sort: Literal["hot", "new", "top", "rising", "controversial", "best"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.RedditPost]:
        """List trending posts

        List trending posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.reddit.trending.posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-trending-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/reddit/trending/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/trending/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, sort: Literal["hot", "new", "top", "rising", "controversial", "best"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.trending.posts.list, one request per page."""
        page: AsyncPage[models.RedditPost] | None = await self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncRedditTrendingResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncRedditTrendingPostsResource(transport, bindings)


class AsyncRedditResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.search = AsyncRedditSearchResource(transport, bindings)
        self.subreddits = AsyncRedditSubredditsResource(transport, bindings)
        self.trending = AsyncRedditTrendingResource(transport, bindings)

    def domain(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncRedditDomainResource:
        """Select the domain resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="domain")
        return AsyncRedditDomainResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncRedditPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="post")
        return AsyncRedditPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> AsyncRedditProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="reddit", resource="profile")
        return AsyncRedditProfileResource(self._transport, {**self._bindings, "identifier": identifier})

    def subreddit(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncRedditSubredditResource:
        """Select the subreddit resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="subreddit")
        return AsyncRedditSubredditResource(self._transport, {**self._bindings, "identifier": identifier})


class AsyncTestDataEntryResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def get(self, *, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TestDataEntry]:
        """Get one synthetic catalog entry

        Example:
            await openhandle.test_data.entry("<id>").get()
        """
        params: dict[str, Any] = {}
        result = await self._transport.request("get", "/v1/test-data/{id}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTestDataResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def entry(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTestDataEntryResource:
        """Select the testDataEntry resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="testData", resource="testDataEntry")
        return AsyncTestDataEntryResource(self._transport, {**self._bindings, "id": identifier})

    async def list(self, *, platform: Literal["instagram", "tiktok", "twitter", "reddit"] | None = None, resource: Literal["profile", "post", "comment", "hashtag", "location", "music", "category", "list", "entity", "subreddit", "rule", "wiki", "trophy"] | None = None, operation: str | None = None, traits: str | None = None, status: int | None = None, error_code: str | None = None, search: str | None = None, limit: int | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TestDataCatalog]:
        """Search the synthetic test-data catalog

        Public, unmetered discovery. Filter by platform, resource, operation, traits, expected status, error code, or text.

        Example:
            await openhandle.test_data.list()
        """
        params: dict[str, Any] = {"platform": platform, "resource": resource, "operation": operation, "traits": traits, "status": status, "errorCode": error_code, "search": search, "limit": limit}
        result = await self._transport.request("get", "/v1/test-data", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokEffectPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List effect posts

        Return a cursor-paginated collection of TikTok posts that use an effect. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.effect("920100000000000001").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-effect-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/effects/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/effects/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.effect.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokEffectResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncTikTokEffectPostsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokEffect]:
        """Get an effect

        Get an effect from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.effect("920100000000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-effect-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/effects/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokEffectsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, ids: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokEffect]:
        """Get effects in a batch

        Get effects in a batch from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.effects.list(ids="950000000007", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-effects-list
        """
        params: dict[str, Any] = {"ids": ids, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/effects", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/effects", self._bindings, params, timeout, max_retries)

    async def items(self, *, ids: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokEffect]:
        """Iterate items lazily across pages of tiktok.effects.list, one request per page."""
        page: AsyncPage[models.TikTokEffect] | None = await self.list(ids=ids, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokHashtagPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List hashtag posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.hashtag("950000000001").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-hashtag-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/hashtags/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/hashtags/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.hashtag.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokHashtagResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncTikTokHashtagPostsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokHashtag]:
        """Get a hashtag

        Return one public hashtag by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.hashtag("950000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-hashtag-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/hashtags/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokLocationPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List location posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.location("950000000002").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-location-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/locations/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/locations/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.location.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokLocationResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncTikTokLocationPostsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokLocation]:
        """Get a location

        Return one public location by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.location("950000000002").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-location-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/locations/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokMusicPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List music posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.music("950000000003").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-music-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/music/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/music/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.music.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokMusicResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncTikTokMusicPostsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokMusic]:
        """Get a music

        Return one public music by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.music("950000000003").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-music-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/music/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokPostCommentRepliesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokComment]:
        """List comment replies

        Return a stable page of replies for one comment. Use find_test_data to discover compatible post and comment IDs. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.post("920100000000000001").comment("920200000000000001").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-comment-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokComment]:
        """Iterate items lazily across pages of tiktok.post.comment.replies.list, one request per page."""
        page: AsyncPage[models.TikTokComment] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokPostCommentResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.replies = AsyncTikTokPostCommentRepliesResource(transport, bindings)


class AsyncTikTokPostCommentsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokComment]:
        """List post comments

        List post comments by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.post("920100000000000001").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-post-comments-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokComment]:
        """Iterate items lazily across pages of tiktok.post.comments.list, one request per page."""
        page: AsyncPage[models.TikTokComment] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokPostResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = AsyncTikTokPostCommentsResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokPostCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="comment")
        return AsyncTikTokPostCommentResource(self._transport, {**self._bindings, "comment_id": identifier})

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokPost]:
        """Get a post

        Get a post by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.post("920100000000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokProfileFollowersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokProfile]:
        """List profile followers

        Return a cursor-paginated collection of Tiktok profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").followers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-followers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/followers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/followers", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokProfile]:
        """Iterate items lazily across pages of tiktok.profile.followers.list, one request per page."""
        page: AsyncPage[models.TikTokProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokProfileFollowingResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokProfile]:
        """List profile following

        Return a cursor-paginated collection of Tiktok profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").following.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-following-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/following", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/following", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokProfile]:
        """Iterate items lazily across pages of tiktok.profile.following.list, one request per page."""
        page: AsyncPage[models.TikTokProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokProfileLikedPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List profile liked posts

        Return a cursor-paginated Tiktok profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").liked_posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-liked-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/liked-posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/liked-posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.profile.likedPosts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokProfilePlaylistPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List playlist posts

        Return a cursor-paginated collection of posts in a public TikTok profile playlist. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").playlist("950000000006").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-playlist-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/playlists/{playlist_id}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/playlists/{playlist_id}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.profile.playlist.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokProfilePlaylistResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncTikTokProfilePlaylistPostsResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokPlaylist]:
        """Get a playlist

        Get a playlist from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").playlist("950000000006").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-playlist-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/playlists/{playlist_id}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokProfilePlaylistsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPlaylist]:
        """List profile playlists

        List profile playlists from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").playlists.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-playlists-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/playlists", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/playlists", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPlaylist]:
        """Iterate items lazily across pages of tiktok.profile.playlists.list, one request per page."""
        page: AsyncPage[models.TikTokPlaylist] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokProfilePostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List profile posts

        Return a stable page of posts for one public profile. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor, "since": since}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.profile.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, since=since, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokProfileResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.followers = AsyncTikTokProfileFollowersResource(transport, bindings)
        self.following = AsyncTikTokProfileFollowingResource(transport, bindings)
        self.liked_posts = AsyncTikTokProfileLikedPostsResource(transport, bindings)
        self.playlists = AsyncTikTokProfilePlaylistsResource(transport, bindings)
        self.posts = AsyncTikTokProfilePostsResource(transport, bindings)

    def playlist(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokProfilePlaylistResource:
        """Select the playlist resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="playlist")
        return AsyncTikTokProfilePlaylistResource(self._transport, {**self._bindings, "playlist_id": identifier})

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokProfile]:
        """Get a profile

        Return one public profile. Use @username or a platform ID as the identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.profile("@pixel_orchard_tt_test").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/tiktok/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTikTokSearchHashtagsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokHashtag]:
        """Search Tiktok hashtags

        Search public Tiktok hashtags with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.search.hashtags.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-hashtags-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/search/hashtags", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/hashtags", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokHashtag]:
        """Iterate items lazily across pages of tiktok.search.hashtags.list, one request per page."""
        page: AsyncPage[models.TikTokHashtag] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokSearchLocationsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokLocation]:
        """Search Tiktok locations

        Search public Tiktok locations with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.search.locations.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-locations-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/search/locations", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/locations", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokLocation]:
        """Iterate items lazily across pages of tiktok.search.locations.list, one request per page."""
        page: AsyncPage[models.TikTokLocation] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokSearchMusicResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokMusic]:
        """Search Tiktok music

        Search public Tiktok music with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.search.music.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-music-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/search/music", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/music", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokMusic]:
        """Iterate items lazily across pages of tiktok.search.music.list, one request per page."""
        page: AsyncPage[models.TikTokMusic] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokSearchPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """Search Tiktok posts

        Search public Tiktok posts with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.search.posts.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-posts-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/search/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.search.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokSearchProfilesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokProfile]:
        """Search Tiktok profiles

        Search public Tiktok profiles with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.search.profiles.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-profiles-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/search/profiles", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/profiles", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokProfile]:
        """Iterate items lazily across pages of tiktok.search.profiles.list, one request per page."""
        page: AsyncPage[models.TikTokProfile] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokSearchResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.hashtags = AsyncTikTokSearchHashtagsResource(transport, bindings)
        self.locations = AsyncTikTokSearchLocationsResource(transport, bindings)
        self.music = AsyncTikTokSearchMusicResource(transport, bindings)
        self.posts = AsyncTikTokSearchPostsResource(transport, bindings)
        self.profiles = AsyncTikTokSearchProfilesResource(transport, bindings)


class AsyncTikTokTrendingCategoriesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokCategory]:
        """List trending categories

        Return TikTok trending categories. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.trending.categories.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-trending-categories-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/trending/categories", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/trending/categories", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokCategory]:
        """Iterate items lazily across pages of tiktok.trending.categories.list, one request per page."""
        page: AsyncPage[models.TikTokCategory] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokTrendingMusicResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokMusic]:
        """List trending music

        Return TikTok's public Top 50 music chart for the configured provider region. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.trending.music.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-trending-music-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/trending/music", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/trending/music", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokMusic]:
        """Iterate items lazily across pages of tiktok.trending.music.list, one request per page."""
        page: AsyncPage[models.TikTokMusic] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokTrendingPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TikTokPost]:
        """List trending posts

        Return TikTok's public recommended feed. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.tiktok.trending.posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-trending-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/tiktok/trending/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/trending/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.trending.posts.list, one request per page."""
        page: AsyncPage[models.TikTokPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTikTokTrendingResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.categories = AsyncTikTokTrendingCategoriesResource(transport, bindings)
        self.music = AsyncTikTokTrendingMusicResource(transport, bindings)
        self.posts = AsyncTikTokTrendingPostsResource(transport, bindings)


class AsyncTikTokResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.effects = AsyncTikTokEffectsResource(transport, bindings)
        self.search = AsyncTikTokSearchResource(transport, bindings)
        self.trending = AsyncTikTokTrendingResource(transport, bindings)

    def effect(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokEffectResource:
        """Select the effect resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="effect")
        return AsyncTikTokEffectResource(self._transport, {**self._bindings, "identifier": identifier})

    def hashtag(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokHashtagResource:
        """Select the hashtag resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="hashtag")
        return AsyncTikTokHashtagResource(self._transport, {**self._bindings, "identifier": identifier})

    def location(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokLocationResource:
        """Select the location resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="location")
        return AsyncTikTokLocationResource(self._transport, {**self._bindings, "identifier": identifier})

    def music(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokMusicResource:
        """Select the music resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="music")
        return AsyncTikTokMusicResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTikTokPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="post")
        return AsyncTikTokPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> AsyncTikTokProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="tiktok", resource="profile")
        return AsyncTikTokProfileResource(self._transport, {**self._bindings, "identifier": identifier})


class AsyncTwitterListPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """List Twitter list posts

        Return the cursor-paginated timeline for a Twitter list. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.list("950000000005").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-list-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/lists/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/lists/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.list.posts.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterListResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = AsyncTwitterListPostsResource(transport, bindings)


class AsyncTwitterPostCommentRepliesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """List comment replies

        Return a stable page of replies for one comment. Use find_test_data to discover compatible post and comment IDs. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.post("940100000000000001").comment("940200000000000001").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-comment-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.post.comment.replies.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterPostCommentResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.replies = AsyncTwitterPostCommentRepliesResource(transport, bindings)


class AsyncTwitterPostCommentsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """List post comments

        List post comments by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.post("940100000000000001").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-post-comments-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.post.comments.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterPostRepostersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterProfile]:
        """List post reposters

        Return public profiles that interacted with a Twitter post. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.post("940100000000000001").reposters.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-post-reposters-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/posts/{identifier}/reposters", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/posts/{identifier}/reposters", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.post.reposters.list, one request per page."""
        page: AsyncPage[models.TwitterProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterPostResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = AsyncTwitterPostCommentsResource(transport, bindings)
        self.reposters = AsyncTwitterPostRepostersResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTwitterPostCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="twitter", resource="comment")
        return AsyncTwitterPostCommentResource(self._transport, {**self._bindings, "comment_id": identifier})

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TwitterPost]:
        """Get a post

        Get a post by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.post("940100000000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/twitter/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTwitterProfileFollowersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterProfile]:
        """List profile followers

        Return a cursor-paginated collection of Twitter profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.profile("@copperfield_lab_x_test").followers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-followers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/profiles/{identifier}/followers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/followers", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.profile.followers.list, one request per page."""
        page: AsyncPage[models.TwitterProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterProfileFollowingResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterProfile]:
        """List profile following

        Return a cursor-paginated collection of Twitter profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.profile("@copperfield_lab_x_test").following.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-following-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/profiles/{identifier}/following", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/following", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.profile.following.list, one request per page."""
        page: AsyncPage[models.TwitterProfile] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterProfileMediaResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """List profile media

        Return a cursor-paginated Twitter profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.profile("@copperfield_lab_x_test").media.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-media-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/profiles/{identifier}/media", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/media", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.profile.media.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterProfilePostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """List profile posts

        Return a stable page of posts for one public profile. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.profile("@copperfield_lab_x_test").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor, "since": since}
        result = await self._transport.request("get", "/v1/twitter/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.profile.posts.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(freshness=freshness, cursor=cursor, since=since, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterProfileRepliesResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """List profile replies

        Return a cursor-paginated Twitter profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.profile("@copperfield_lab_x_test").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/profiles/{identifier}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/replies", self._bindings, params, timeout, max_retries)

    async def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.profile.replies.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterProfileResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.followers = AsyncTwitterProfileFollowersResource(transport, bindings)
        self.following = AsyncTwitterProfileFollowingResource(transport, bindings)
        self.media = AsyncTwitterProfileMediaResource(transport, bindings)
        self.posts = AsyncTwitterProfilePostsResource(transport, bindings)
        self.replies = AsyncTwitterProfileRepliesResource(transport, bindings)

    async def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TwitterProfile]:
        """Get a profile

        Return one public profile. Use @username or a platform ID as the identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.profile("@copperfield_lab_x_test").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = await self._transport.request("get", "/v1/twitter/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class AsyncTwitterSearchMediaResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, type: Literal["image", "video"], freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """Search Twitter media

        Search public Twitter media with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.search.media.list(q="synthetic", type="image", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-media-list
        """
        params: dict[str, Any] = {"q": q, "type": type, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/search/media", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/media", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, type: Literal["image", "video"], freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.search.media.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(q=q, type=type, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterSearchPostsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, sort: Literal["latest", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterPost]:
        """Search Twitter posts

        Search public Twitter posts with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.search.posts.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-posts-list
        """
        params: dict[str, Any] = {"q": q, "sort": sort, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/search/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/posts", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, sort: Literal["latest", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.search.posts.list, one request per page."""
        page: AsyncPage[models.TwitterPost] | None = await self.list(q=q, sort=sort, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterSearchSuggestionsResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterProfile]:
        """Search suggestions

        Search suggestions from public Twitter data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.search.suggestions.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-suggestions-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/search/suggestions", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/suggestions", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.search.suggestions.list, one request per page."""
        page: AsyncPage[models.TwitterProfile] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterSearchUsersResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    async def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncPage[models.TwitterProfile]:
        """Search Twitter users

        Search public Twitter users with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            await openhandle.twitter.search.users.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-users-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = await self._transport.request("get", "/v1/twitter/search/users", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/users", self._bindings, params, timeout, max_retries)

    async def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> AsyncIterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.search.users.list, one request per page."""
        page: AsyncPage[models.TwitterProfile] | None = await self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            for item in page.data:
                yield item
            page = await page.next()


class AsyncTwitterSearchResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.media = AsyncTwitterSearchMediaResource(transport, bindings)
        self.posts = AsyncTwitterSearchPostsResource(transport, bindings)
        self.suggestions = AsyncTwitterSearchSuggestionsResource(transport, bindings)
        self.users = AsyncTwitterSearchUsersResource(transport, bindings)


class AsyncTwitterResource:
    def __init__(self, transport: AsyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.search = AsyncTwitterSearchResource(transport, bindings)

    def list(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTwitterListResource:
        """Select the list resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="twitter", resource="list")
        return AsyncTwitterListResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> AsyncTwitterPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="twitter", resource="post")
        return AsyncTwitterPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> AsyncTwitterProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="twitter", resource="profile")
        return AsyncTwitterProfileResource(self._transport, {**self._bindings, "identifier": identifier})


class AsyncGeneratedClient:
    def __init__(self, transport: AsyncTransport) -> None:
        self._transport = transport
        self.instagram = AsyncInstagramResource(transport, {})
        self.reddit = AsyncRedditResource(transport, {})
        self.test_data = AsyncTestDataResource(transport, {})
        self.tiktok = AsyncTikTokResource(transport, {})
        self.twitter = AsyncTwitterResource(transport, {})

    async def fetch(self, url: str, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.FetchResource]:
        """Fetch the resource at a social URL

        Parse a supported social URL locally, then return the same typed resource as its canonical Openhandle operation. TikTok short links such as tiktok.com/t/… and vm.tiktok.com/… are expanded through their redirect first. The underlying data read is metered once.

        Example:
            await openhandle.fetch("https://www.instagram.com/openai/", freshness="24h")
        """
        if not url or not url.strip():
            raise TypeError("fetch(url) requires a non-empty social URL.")
        body: dict[str, Any] = {"url": url}
        if freshness is not None:
            body["freshness"] = freshness
        result = await self._transport.request("post", "/v1/urls/fetch", {}, {}, json_body=body, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)
