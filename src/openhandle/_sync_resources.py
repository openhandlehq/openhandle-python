# Generated by scripts/generate.py. Do not edit.

from __future__ import annotations

from collections.abc import Iterator
from datetime import datetime
from typing import Any, Literal

from openhandle import models
from openhandle._references import resolve_selector
from openhandle._response import Page, Response
from openhandle._transport import SyncTransport
from openhandle._types import Freshness


class InstagramCommentLikersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, media_id: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfileSummary]:
        """List comment likers

        List comment likers from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.comment("910100000001").likers.list(media_id="910100000001", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-comment-likers-list
        """
        params: dict[str, Any] = {"media_id": media_id, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/comments/{identifier}/likers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/comments/{identifier}/likers", self._bindings, params, timeout, max_retries)

    def items(self, *, media_id: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfileSummary]:
        """Iterate items lazily across pages of instagram.comment.likers.list, one request per page."""
        page: Page[models.InstagramProfileSummary] | None = self.list(media_id=media_id, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramCommentResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.likers = InstagramCommentLikersResource(transport, bindings)


class InstagramHashtagPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List hashtag posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.hashtag("syntheticvolume").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-hashtag-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/hashtags/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/hashtags/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.hashtag.posts.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(sort=sort, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramHashtagReelsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List hashtag reels

        Return a cursor-paginated collection of Instagram reels for a hashtag. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.hashtag("910100000001").reels.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-hashtag-reels-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/hashtags/{identifier}/reels", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/hashtags/{identifier}/reels", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.hashtag.reels.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramHashtagResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = InstagramHashtagPostsResource(transport, bindings)
        self.reels = InstagramHashtagReelsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramHashtag]:
        """Get a hashtag

        Return one public hashtag by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.hashtag("syntheticvolume").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-hashtag-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/hashtags/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramHighlightResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramHighlight]:
        """Get a highlight

        Get a highlight from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.highlight("910100000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-highlight-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/highlights/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramLocationPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List location posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.location("950000000002").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-location-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/locations/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/locations/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["recent", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.location.posts.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(sort=sort, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramLocationResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = InstagramLocationPostsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramLocation]:
        """Get a location

        Return one public location by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.location("950000000002").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-location-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/locations/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramMusicStreamResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramMusicStream]:
        """List music stream pages

        List music stream pages from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.music("910100000001").stream.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-music-stream-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/music/{identifier}/stream", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/music/{identifier}/stream", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramMusicStream]:
        """Iterate items lazily across pages of instagram.music.stream.list, one request per page."""
        page: Page[models.InstagramMusicStream] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramMusicResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.stream = InstagramMusicStreamResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramMusic]:
        """Get a music

        Return one public music by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.music("950000000003").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-music-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/music/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostClipMetadataResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramClipMetadata]:
        """Get clip metadata

        Get clip metadata from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").clip_metadata.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-clip-metadata-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/clip-metadata", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostCommentRepliesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramComment]:
        """List comment replies

        Return a stable page of replies for one comment. Use find_test_data to discover compatible post and comment IDs. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").comment("910200000001").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-comment-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramComment]:
        """Iterate items lazily across pages of instagram.post.comment.replies.list, one request per page."""
        page: Page[models.InstagramComment] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramPostCommentResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.replies = InstagramPostCommentRepliesResource(transport, bindings)


class InstagramPostCommentMetadataResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramCommentMetadata]:
        """Get comment metadata

        Get comment metadata from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").comment_metadata.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-comment-metadata-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/comment-metadata", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostCommentModerationResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, comment: str, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramCommentModeration]:
        """Check comment moderation

        Check comment moderation from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").comment_moderation.get(comment="synthetic comment", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-comment-moderation-get
        """
        params: dict[str, Any] = {"comment": comment, "freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/comment-moderation", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostCommentsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramComment]:
        """List post comments

        List post comments by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-comments-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramComment]:
        """Iterate items lazily across pages of instagram.post.comments.list, one request per page."""
        page: Page[models.InstagramComment] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramPostInsightsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramPostInsight]:
        """Get post insights

        Get post insights from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").insights.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-insights-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/insights", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostLikersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfile]:
        """List post likers

        Return public profiles that liked an Instagram post. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").likers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-likers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/likers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/likers", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.post.likers.list, one request per page."""
        page: Page[models.InstagramProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramPostOEmbedResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramOEmbed]:
        """Get post oEmbed data

        Get post oEmbed data from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").oembed.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-oembed-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/oembed", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostTaggedUsersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramUserTag]:
        """List tagged users

        Return public profiles tagged in an Instagram post. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").tagged_users.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-tagged-users-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/tagged-users", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/posts/{identifier}/tagged-users", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramUserTag]:
        """Iterate items lazily across pages of instagram.post.taggedUsers.list, one request per page."""
        page: Page[models.InstagramUserTag] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramPostTemplateResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramTemplate]:
        """Get post template

        Get post template from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").template.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-template-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}/template", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramPostResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.clip_metadata = InstagramPostClipMetadataResource(transport, bindings)
        self.comment_metadata = InstagramPostCommentMetadataResource(transport, bindings)
        self.comment_moderation = InstagramPostCommentModerationResource(transport, bindings)
        self.comments = InstagramPostCommentsResource(transport, bindings)
        self.insights = InstagramPostInsightsResource(transport, bindings)
        self.likers = InstagramPostLikersResource(transport, bindings)
        self.oembed = InstagramPostOEmbedResource(transport, bindings)
        self.tagged_users = InstagramPostTaggedUsersResource(transport, bindings)
        self.template = InstagramPostTemplateResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramPostCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="comment")
        return InstagramPostCommentResource(self._transport, {**self._bindings, "comment_id": identifier})

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramPost]:
        """Get a post

        Get a post by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.post("910100000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramProfileAboutResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramProfileAbout]:
        """Get profile about information

        Get profile about information from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").about.get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-about-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/about", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramProfileBusinessRecommendationsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfileSummary]:
        """List business recommendations

        List business recommendations from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").business_recommendations.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-business-recommendations-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/business-recommendations", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/business-recommendations", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfileSummary]:
        """Iterate items lazily across pages of instagram.profile.businessRecommendations.list, one request per page."""
        page: Page[models.InstagramProfileSummary] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileFollowersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfile]:
        """List profile followers

        Return a cursor-paginated collection of Instagram profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").followers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-followers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/followers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/followers", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.followers.list, one request per page."""
        page: Page[models.InstagramProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()

    def search(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfile]:
        """Search profile followers

        Search within a public profile's followers collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").followers.search(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-followers-search
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/followers/search", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/followers/search", self._bindings, params, timeout, max_retries)

    def search_items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.followers.search, one request per page."""
        page: Page[models.InstagramProfile] | None = self.search(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileFollowingResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfile]:
        """List profile following

        Return a cursor-paginated collection of Instagram profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").following.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-following-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/following", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/following", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.following.list, one request per page."""
        page: Page[models.InstagramProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()

    def search(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfile]:
        """Search profile following

        Search within a public profile's following collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").following.search(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-following-search
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/following/search", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/following/search", self._bindings, params, timeout, max_retries)

    def search_items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.profile.following.search, one request per page."""
        page: Page[models.InstagramProfile] | None = self.search(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileHighlightsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramHighlight]:
        """List profile highlights

        List profile highlights from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").highlights.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-highlights-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/highlights", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/highlights", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramHighlight]:
        """Iterate items lazily across pages of instagram.profile.highlights.list, one request per page."""
        page: Page[models.InstagramHighlight] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfilePinnedPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List profile pinned posts

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").pinned_posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-pinned-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/pinned-posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/pinned-posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.pinnedPosts.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfilePostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List profile posts

        Return a stable page of posts for one public profile. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor, "since": since}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.posts.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, since=since, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileReelsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List profile reels

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").reels.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-reels-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/reels", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/reels", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.reels.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileRepostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List profile reposts

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").reposts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-reposts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/reposts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/reposts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.reposts.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileStoriesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramStory]:
        """List profile stories

        List profile stories from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").stories.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-stories-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/stories", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/stories", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramStory]:
        """Iterate items lazily across pages of instagram.profile.stories.list, one request per page."""
        page: Page[models.InstagramStory] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileSuggestedResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramProfile]:
        """List profile suggested

        Return the current collection of Instagram profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").suggested.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-suggested-list
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/suggested", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramProfileTaggedPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List profile tagged posts

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").tagged_posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-tagged-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/tagged-posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/tagged-posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.taggedPosts.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileVideosResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """List profile videos

        Return a cursor-paginated Instagram profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").videos.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-videos-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}/videos", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/profiles/{identifier}/videos", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.profile.videos.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramProfileResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.about = InstagramProfileAboutResource(transport, bindings)
        self.business_recommendations = InstagramProfileBusinessRecommendationsResource(transport, bindings)
        self.followers = InstagramProfileFollowersResource(transport, bindings)
        self.following = InstagramProfileFollowingResource(transport, bindings)
        self.highlights = InstagramProfileHighlightsResource(transport, bindings)
        self.pinned_posts = InstagramProfilePinnedPostsResource(transport, bindings)
        self.posts = InstagramProfilePostsResource(transport, bindings)
        self.reels = InstagramProfileReelsResource(transport, bindings)
        self.reposts = InstagramProfileRepostsResource(transport, bindings)
        self.stories = InstagramProfileStoriesResource(transport, bindings)
        self.suggested = InstagramProfileSuggestedResource(transport, bindings)
        self.tagged_posts = InstagramProfileTaggedPostsResource(transport, bindings)
        self.videos = InstagramProfileVideosResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramProfile]:
        """Get a profile

        Return one public profile. Use @username or a platform ID as the identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.profile("@northstar_forge_test").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramSearchHashtagsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramHashtag]:
        """Search Instagram hashtags

        Search public Instagram hashtags with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.search.hashtags.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-hashtags-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/search/hashtags", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/hashtags", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramHashtag]:
        """Iterate items lazily across pages of instagram.search.hashtags.list, one request per page."""
        page: Page[models.InstagramHashtag] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramSearchMusicResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramMusic]:
        """Search Instagram music

        Search public Instagram music with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.search.music.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-music-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/search/music", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/music", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramMusic]:
        """Iterate items lazily across pages of instagram.search.music.list, one request per page."""
        page: Page[models.InstagramMusic] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramSearchPlacesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramLocation]:
        """Search Instagram places

        Search public Instagram places with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.search.places.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-places-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/search/places", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/places", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramLocation]:
        """Iterate items lazily across pages of instagram.search.places.list, one request per page."""
        page: Page[models.InstagramLocation] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramSearchProfilesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramProfile]:
        """Search Instagram profiles

        Search public Instagram profiles with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.search.profiles.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-profiles-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/search/profiles", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/profiles", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramProfile]:
        """Iterate items lazily across pages of instagram.search.profiles.list, one request per page."""
        page: Page[models.InstagramProfile] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramSearchReelsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramPost]:
        """Search Instagram reels

        Search public Instagram reels with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.search.reels.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-reels-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/search/reels", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/reels", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramPost]:
        """Iterate items lazily across pages of instagram.search.reels.list, one request per page."""
        page: Page[models.InstagramPost] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramSearchTopResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.InstagramSearchResult]:
        """Search all Instagram results

        Search all Instagram results from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.search.top.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-search-top-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/instagram/search/top", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/instagram/search/top", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.InstagramSearchResult]:
        """Iterate items lazily across pages of instagram.search.top.list, one request per page."""
        page: Page[models.InstagramSearchResult] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class InstagramSearchResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.hashtags = InstagramSearchHashtagsResource(transport, bindings)
        self.music = InstagramSearchMusicResource(transport, bindings)
        self.places = InstagramSearchPlacesResource(transport, bindings)
        self.profiles = InstagramSearchProfilesResource(transport, bindings)
        self.reels = InstagramSearchReelsResource(transport, bindings)
        self.top = InstagramSearchTopResource(transport, bindings)


class InstagramStoryResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.InstagramStory]:
        """Get a story

        Get a story from public Instagram data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.instagram.story("910100000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/instagram-story-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/instagram/stories/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class InstagramResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.search = InstagramSearchResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="comment")
        return InstagramCommentResource(self._transport, {**self._bindings, "identifier": identifier})

    def hashtag(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramHashtagResource:
        """Select the hashtag resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="hashtag")
        return InstagramHashtagResource(self._transport, {**self._bindings, "identifier": identifier})

    def highlight(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramHighlightResource:
        """Select the highlight resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="highlight")
        return InstagramHighlightResource(self._transport, {**self._bindings, "identifier": identifier})

    def location(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramLocationResource:
        """Select the location resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="location")
        return InstagramLocationResource(self._transport, {**self._bindings, "identifier": identifier})

    def music(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramMusicResource:
        """Select the music resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="music")
        return InstagramMusicResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="post")
        return InstagramPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> InstagramProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="instagram", resource="profile")
        return InstagramProfileResource(self._transport, {**self._bindings, "identifier": identifier})

    def story(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> InstagramStoryResource:
        """Select the story resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="instagram", resource="story")
        return InstagramStoryResource(self._transport, {**self._bindings, "identifier": identifier})


class RedditDomainPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditPost]:
        """List domain posts

        List domain posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.domain("example.com").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-domain-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/domains/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/domains/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.domain.posts.list, one request per page."""
        page: Page[models.RedditPost] | None = self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditDomainResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = RedditDomainPostsResource(transport, bindings)


class RedditPostCommentsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["confidence", "top", "new", "controversial", "old", "qa"] | None = None, limit: int | None = None, depth: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditComment]:
        """List post comments

        List post comments from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.post("synthetic").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-post-comments-list
        """
        params: dict[str, Any] = {"sort": sort, "limit": limit, "depth": depth, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["confidence", "top", "new", "controversial", "old", "qa"] | None = None, limit: int | None = None, depth: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditComment]:
        """Iterate items lazily across pages of reddit.post.comments.list, one request per page."""
        page: Page[models.RedditComment] | None = self.list(sort=sort, limit=limit, depth=depth, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditPostDuplicatesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditPost]:
        """List duplicate posts

        List duplicate posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.post("synthetic").duplicates.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-post-duplicates-list
        """
        params: dict[str, Any] = {"limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/posts/{identifier}/duplicates", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/posts/{identifier}/duplicates", self._bindings, params, timeout, max_retries)

    def items(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.post.duplicates.list, one request per page."""
        page: Page[models.RedditPost] | None = self.list(limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditPostResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = RedditPostCommentsResource(transport, bindings)
        self.duplicates = RedditPostDuplicatesResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditPost]:
        """Get a post

        Get a post from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.post("synthetic").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/reddit/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class RedditProfileCommentsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditComment]:
        """List profile comments

        List profile comments from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.profile("@synthetic_reddit").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-comments-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/profiles/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/profiles/{identifier}/comments", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditComment]:
        """Iterate items lazily across pages of reddit.profile.comments.list, one request per page."""
        page: Page[models.RedditComment] | None = self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditProfileModeratedResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditSubreddit]:
        """List moderated subreddits

        List moderated subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.profile("@synthetic_reddit").moderated.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-moderated-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/profiles/{identifier}/moderated", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/profiles/{identifier}/moderated", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.profile.moderated.list, one request per page."""
        page: Page[models.RedditSubreddit] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditProfilePostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditPost]:
        """List profile posts

        List profile posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.profile("@synthetic_reddit").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["new", "hot", "top", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.profile.posts.list, one request per page."""
        page: Page[models.RedditPost] | None = self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditProfileTrophiesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditTrophy]:
        """List profile trophies

        List profile trophies from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.profile("@synthetic_reddit").trophies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-trophies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/profiles/{identifier}/trophies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/profiles/{identifier}/trophies", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditTrophy]:
        """Iterate items lazily across pages of reddit.profile.trophies.list, one request per page."""
        page: Page[models.RedditTrophy] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditProfileResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = RedditProfileCommentsResource(transport, bindings)
        self.moderated = RedditProfileModeratedResource(transport, bindings)
        self.posts = RedditProfilePostsResource(transport, bindings)
        self.trophies = RedditProfileTrophiesResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditProfile]:
        """Get a profile

        Return one public Reddit profile. Use @username or a t2_ profile ID. An ID resolves only after the profile was read by username once. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.profile("@synthetic_reddit").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/reddit/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class RedditSearchPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, sort: Literal["relevance", "hot", "top", "new", "comments"] | None = None, subreddit: str | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditPost]:
        """Search posts

        Search posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.search.posts.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-search-posts-list
        """
        params: dict[str, Any] = {"q": q, "sort": sort, "subreddit": subreddit, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/search/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/search/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, sort: Literal["relevance", "hot", "top", "new", "comments"] | None = None, subreddit: str | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.search.posts.list, one request per page."""
        page: Page[models.RedditPost] | None = self.list(q=q, sort=sort, subreddit=subreddit, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSearchProfilesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditProfile]:
        """Search profiles

        Search profiles from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.search.profiles.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-search-profiles-list
        """
        params: dict[str, Any] = {"q": q, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/search/profiles", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/search/profiles", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditProfile]:
        """Iterate items lazily across pages of reddit.search.profiles.list, one request per page."""
        page: Page[models.RedditProfile] | None = self.list(q=q, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSearchSubredditsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditSubreddit]:
        """Search subreddits

        Search subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.search.subreddits.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-search-subreddits-list
        """
        params: dict[str, Any] = {"q": q, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/search/subreddits", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/search/subreddits", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.search.subreddits.list, one request per page."""
        page: Page[models.RedditSubreddit] | None = self.list(q=q, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSearchResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = RedditSearchPostsResource(transport, bindings)
        self.profiles = RedditSearchProfilesResource(transport, bindings)
        self.subreddits = RedditSearchSubredditsResource(transport, bindings)


class RedditSubredditPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditPost]:
        """List subreddit posts

        List subreddit posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddit("synthetic").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/subreddits/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["hot", "new", "top", "rising", "controversial"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.subreddit.posts.list, one request per page."""
        page: Page[models.RedditPost] | None = self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSubredditRulesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditRule]:
        """List subreddit rules

        List subreddit rules from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddit("synthetic").rules.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-rules-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/subreddits/{identifier}/rules", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/{identifier}/rules", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditRule]:
        """Iterate items lazily across pages of reddit.subreddit.rules.list, one request per page."""
        page: Page[models.RedditRule] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSubredditWikiPageResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditWikiPage]:
        """Get a wiki page

        Get a wiki page from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddit("synthetic").wiki_page("index").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-wiki-page-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/reddit/subreddits/{identifier}/wiki-pages/{page}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class RedditSubredditWikiPagesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditWikiPage]:
        """List wiki pages

        List wiki pages from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddit("synthetic").wiki_pages.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-wiki-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/subreddits/{identifier}/wiki-pages", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/{identifier}/wiki-pages", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditWikiPage]:
        """Iterate items lazily across pages of reddit.subreddit.wikiPages.list, one request per page."""
        page: Page[models.RedditWikiPage] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSubredditResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = RedditSubredditPostsResource(transport, bindings)
        self.rules = RedditSubredditRulesResource(transport, bindings)
        self.wiki_pages = RedditSubredditWikiPagesResource(transport, bindings)

    def wiki_page(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> RedditSubredditWikiPageResource:
        """Select the wikiPage resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="wikiPage")
        return RedditSubredditWikiPageResource(self._transport, {**self._bindings, "page": identifier})

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.RedditSubreddit]:
        """Get a subreddit

        Get a subreddit from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddit("synthetic").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddit-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/reddit/subreddits/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class RedditSubredditsNewResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditSubreddit]:
        """List new subreddits

        List new subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddits.new.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddits-new-list
        """
        params: dict[str, Any] = {"limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/subreddits/new", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/new", self._bindings, params, timeout, max_retries)

    def items(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.subreddits.new.list, one request per page."""
        page: Page[models.RedditSubreddit] | None = self.list(limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSubredditsPopularResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditSubreddit]:
        """List popular subreddits

        List popular subreddits from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.subreddits.popular.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-subreddits-popular-list
        """
        params: dict[str, Any] = {"limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/subreddits/popular", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/subreddits/popular", self._bindings, params, timeout, max_retries)

    def items(self, *, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditSubreddit]:
        """Iterate items lazily across pages of reddit.subreddits.popular.list, one request per page."""
        page: Page[models.RedditSubreddit] | None = self.list(limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditSubredditsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.new = RedditSubredditsNewResource(transport, bindings)
        self.popular = RedditSubredditsPopularResource(transport, bindings)


class RedditTrendingPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, sort: Literal["hot", "new", "top", "rising", "controversial", "best"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.RedditPost]:
        """List trending posts

        List trending posts from public Reddit data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.reddit.trending.posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/reddit-trending-posts-list
        """
        params: dict[str, Any] = {"sort": sort, "t": t, "limit": limit, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/reddit/trending/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/reddit/trending/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, sort: Literal["hot", "new", "top", "rising", "controversial", "best"] | None = None, t: Literal["hour", "day", "week", "month", "year", "all"] | None = None, limit: int | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.RedditPost]:
        """Iterate items lazily across pages of reddit.trending.posts.list, one request per page."""
        page: Page[models.RedditPost] | None = self.list(sort=sort, t=t, limit=limit, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class RedditTrendingResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = RedditTrendingPostsResource(transport, bindings)


class RedditResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.search = RedditSearchResource(transport, bindings)
        self.subreddits = RedditSubredditsResource(transport, bindings)
        self.trending = RedditTrendingResource(transport, bindings)

    def domain(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> RedditDomainResource:
        """Select the domain resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="domain")
        return RedditDomainResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> RedditPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="post")
        return RedditPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> RedditProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="reddit", resource="profile")
        return RedditProfileResource(self._transport, {**self._bindings, "identifier": identifier})

    def subreddit(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> RedditSubredditResource:
        """Select the subreddit resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="reddit", resource="subreddit")
        return RedditSubredditResource(self._transport, {**self._bindings, "identifier": identifier})


class TestDataEntryResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def get(self, *, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TestDataEntry]:
        """Get one synthetic catalog entry

        Example:
            openhandle.test_data.entry("<id>").get()
        """
        params: dict[str, Any] = {}
        result = self._transport.request("get", "/v1/test-data/{id}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TestDataResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def entry(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TestDataEntryResource:
        """Select the testDataEntry resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="testData", resource="testDataEntry")
        return TestDataEntryResource(self._transport, {**self._bindings, "id": identifier})

    def list(self, *, platform: Literal["instagram", "tiktok", "twitter", "reddit"] | None = None, resource: Literal["profile", "post", "comment", "hashtag", "location", "music", "category", "list", "entity", "subreddit", "rule", "wiki", "trophy"] | None = None, operation: str | None = None, traits: str | None = None, status: int | None = None, error_code: str | None = None, search: str | None = None, limit: int | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TestDataCatalog]:
        """Search the synthetic test-data catalog

        Public, unmetered discovery. Filter by platform, resource, operation, traits, expected status, error code, or text.

        Example:
            openhandle.test_data.list()
        """
        params: dict[str, Any] = {"platform": platform, "resource": resource, "operation": operation, "traits": traits, "status": status, "errorCode": error_code, "search": search, "limit": limit}
        result = self._transport.request("get", "/v1/test-data", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokEffectPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List effect posts

        Return a cursor-paginated collection of TikTok posts that use an effect. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.effect("920100000000000001").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-effect-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/effects/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/effects/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.effect.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokEffectResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = TikTokEffectPostsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokEffect]:
        """Get an effect

        Get an effect from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.effect("920100000000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-effect-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/effects/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokEffectsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, ids: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokEffect]:
        """Get effects in a batch

        Get effects in a batch from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.effects.list(ids="950000000007", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-effects-list
        """
        params: dict[str, Any] = {"ids": ids, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/effects", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/effects", self._bindings, params, timeout, max_retries)

    def items(self, *, ids: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokEffect]:
        """Iterate items lazily across pages of tiktok.effects.list, one request per page."""
        page: Page[models.TikTokEffect] | None = self.list(ids=ids, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokHashtagPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List hashtag posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.hashtag("950000000001").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-hashtag-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/hashtags/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/hashtags/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.hashtag.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokHashtagResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = TikTokHashtagPostsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokHashtag]:
        """Get a hashtag

        Return one public hashtag by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.hashtag("950000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-hashtag-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/hashtags/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokLocationPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List location posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.location("950000000002").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-location-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/locations/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/locations/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.location.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokLocationResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = TikTokLocationPostsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokLocation]:
        """Get a location

        Return one public location by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.location("950000000002").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-location-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/locations/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokMusicPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List music posts

        Return a cursor-paginated collection of public posts. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.music("950000000003").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-music-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/music/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/music/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.music.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokMusicResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = TikTokMusicPostsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokMusic]:
        """Get a music

        Return one public music by its native identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.music("950000000003").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-music-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/music/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokPostCommentRepliesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokComment]:
        """List comment replies

        Return a stable page of replies for one comment. Use find_test_data to discover compatible post and comment IDs. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.post("920100000000000001").comment("920200000000000001").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-comment-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokComment]:
        """Iterate items lazily across pages of tiktok.post.comment.replies.list, one request per page."""
        page: Page[models.TikTokComment] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokPostCommentResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.replies = TikTokPostCommentRepliesResource(transport, bindings)


class TikTokPostCommentsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokComment]:
        """List post comments

        List post comments by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.post("920100000000000001").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-post-comments-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokComment]:
        """Iterate items lazily across pages of tiktok.post.comments.list, one request per page."""
        page: Page[models.TikTokComment] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokPostResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = TikTokPostCommentsResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokPostCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="comment")
        return TikTokPostCommentResource(self._transport, {**self._bindings, "comment_id": identifier})

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokPost]:
        """Get a post

        Get a post by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.post("920100000000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokProfileFollowersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokProfile]:
        """List profile followers

        Return a cursor-paginated collection of Tiktok profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").followers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-followers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/followers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/followers", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokProfile]:
        """Iterate items lazily across pages of tiktok.profile.followers.list, one request per page."""
        page: Page[models.TikTokProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokProfileFollowingResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokProfile]:
        """List profile following

        Return a cursor-paginated collection of Tiktok profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").following.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-following-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/following", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/following", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokProfile]:
        """Iterate items lazily across pages of tiktok.profile.following.list, one request per page."""
        page: Page[models.TikTokProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokProfileLikedPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List profile liked posts

        Return a cursor-paginated Tiktok profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").liked_posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-liked-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/liked-posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/liked-posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.profile.likedPosts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokProfilePlaylistPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List playlist posts

        Return a cursor-paginated collection of posts in a public TikTok profile playlist. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").playlist("950000000006").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-playlist-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/playlists/{playlist_id}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/playlists/{playlist_id}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.profile.playlist.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokProfilePlaylistResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = TikTokProfilePlaylistPostsResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokPlaylist]:
        """Get a playlist

        Get a playlist from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").playlist("950000000006").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-playlist-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/playlists/{playlist_id}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokProfilePlaylistsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPlaylist]:
        """List profile playlists

        List profile playlists from public Tiktok data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").playlists.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-playlists-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/playlists", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/playlists", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPlaylist]:
        """Iterate items lazily across pages of tiktok.profile.playlists.list, one request per page."""
        page: Page[models.TikTokPlaylist] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokProfilePostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List profile posts

        Return a stable page of posts for one public profile. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor, "since": since}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.profile.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, since=since, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokProfileResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.followers = TikTokProfileFollowersResource(transport, bindings)
        self.following = TikTokProfileFollowingResource(transport, bindings)
        self.liked_posts = TikTokProfileLikedPostsResource(transport, bindings)
        self.playlists = TikTokProfilePlaylistsResource(transport, bindings)
        self.posts = TikTokProfilePostsResource(transport, bindings)

    def playlist(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokProfilePlaylistResource:
        """Select the playlist resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="playlist")
        return TikTokProfilePlaylistResource(self._transport, {**self._bindings, "playlist_id": identifier})

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokProfile]:
        """Get a profile

        Return one public profile. Use @username or a platform ID as the identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.profile("@pixel_orchard_tt_test").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokSearchHashtagsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokHashtag]:
        """Search Tiktok hashtags

        Search public Tiktok hashtags with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.search.hashtags.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-hashtags-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/search/hashtags", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/hashtags", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokHashtag]:
        """Iterate items lazily across pages of tiktok.search.hashtags.list, one request per page."""
        page: Page[models.TikTokHashtag] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokSearchLocationsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokLocation]:
        """Search Tiktok locations

        Search public Tiktok locations with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.search.locations.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-locations-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/search/locations", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/locations", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokLocation]:
        """Iterate items lazily across pages of tiktok.search.locations.list, one request per page."""
        page: Page[models.TikTokLocation] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokSearchMusicResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokMusic]:
        """Search Tiktok music

        Search public Tiktok music with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.search.music.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-music-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/search/music", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/music", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokMusic]:
        """Iterate items lazily across pages of tiktok.search.music.list, one request per page."""
        page: Page[models.TikTokMusic] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokSearchPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """Search Tiktok posts

        Search public Tiktok posts with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.search.posts.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-posts-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/search/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.search.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokSearchProfilesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokProfile]:
        """Search Tiktok profiles

        Search public Tiktok profiles with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.search.profiles.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-search-profiles-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/search/profiles", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/search/profiles", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokProfile]:
        """Iterate items lazily across pages of tiktok.search.profiles.list, one request per page."""
        page: Page[models.TikTokProfile] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokSearchResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.hashtags = TikTokSearchHashtagsResource(transport, bindings)
        self.locations = TikTokSearchLocationsResource(transport, bindings)
        self.music = TikTokSearchMusicResource(transport, bindings)
        self.posts = TikTokSearchPostsResource(transport, bindings)
        self.profiles = TikTokSearchProfilesResource(transport, bindings)


class TikTokTrendingCategoriesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokCategory]:
        """List trending categories

        Return TikTok trending categories. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.trending.categories.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-trending-categories-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/trending/categories", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/trending/categories", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokCategory]:
        """Iterate items lazily across pages of tiktok.trending.categories.list, one request per page."""
        page: Page[models.TikTokCategory] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokTrendingMusicResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TikTokMusic]:
        """List trending music

        Return TikTok's public Top 50 music chart for the configured provider region. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.trending.music.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-trending-music-list
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/tiktok/trending/music", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TikTokTrendingPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TikTokPost]:
        """List trending posts

        Return TikTok's public recommended feed. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.tiktok.trending.posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/tiktok-trending-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/tiktok/trending/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/tiktok/trending/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TikTokPost]:
        """Iterate items lazily across pages of tiktok.trending.posts.list, one request per page."""
        page: Page[models.TikTokPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TikTokTrendingResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.categories = TikTokTrendingCategoriesResource(transport, bindings)
        self.music = TikTokTrendingMusicResource(transport, bindings)
        self.posts = TikTokTrendingPostsResource(transport, bindings)


class TikTokResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.effects = TikTokEffectsResource(transport, bindings)
        self.search = TikTokSearchResource(transport, bindings)
        self.trending = TikTokTrendingResource(transport, bindings)

    def effect(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokEffectResource:
        """Select the effect resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="effect")
        return TikTokEffectResource(self._transport, {**self._bindings, "identifier": identifier})

    def hashtag(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokHashtagResource:
        """Select the hashtag resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="hashtag")
        return TikTokHashtagResource(self._transport, {**self._bindings, "identifier": identifier})

    def location(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokLocationResource:
        """Select the location resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="location")
        return TikTokLocationResource(self._transport, {**self._bindings, "identifier": identifier})

    def music(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokMusicResource:
        """Select the music resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="music")
        return TikTokMusicResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TikTokPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="tiktok", resource="post")
        return TikTokPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> TikTokProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="tiktok", resource="profile")
        return TikTokProfileResource(self._transport, {**self._bindings, "identifier": identifier})


class TwitterListPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """List Twitter list posts

        Return the cursor-paginated timeline for a Twitter list. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.list("950000000005").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-list-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/lists/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/lists/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.list.posts.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterListResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.posts = TwitterListPostsResource(transport, bindings)


class TwitterPostCommentRepliesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """List comment replies

        Return a stable page of replies for one comment. Use find_test_data to discover compatible post and comment IDs. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.post("940100000000000001").comment("940200000000000001").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-comment-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/posts/{identifier}/comments/{comment_id}/replies", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.post.comment.replies.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterPostCommentResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.replies = TwitterPostCommentRepliesResource(transport, bindings)


class TwitterPostCommentsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """List post comments

        List post comments by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.post("940100000000000001").comments.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-post-comments-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/posts/{identifier}/comments", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/posts/{identifier}/comments", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.post.comments.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterPostRepostersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterProfile]:
        """List post reposters

        Return public profiles that interacted with a Twitter post. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.post("940100000000000001").reposters.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-post-reposters-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/posts/{identifier}/reposters", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/posts/{identifier}/reposters", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.post.reposters.list, one request per page."""
        page: Page[models.TwitterProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterPostResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.comments = TwitterPostCommentsResource(transport, bindings)
        self.reposters = TwitterPostRepostersResource(transport, bindings)

    def comment(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TwitterPostCommentResource:
        """Select the comment resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="twitter", resource="comment")
        return TwitterPostCommentResource(self._transport, {**self._bindings, "comment_id": identifier})

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TwitterPost]:
        """Get a post

        Get a post by a canonical platform identifier. URLs must be converted before calling a data endpoint. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.post("940100000000000001").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-post-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/twitter/posts/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TwitterProfileFollowersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterProfile]:
        """List profile followers

        Return a cursor-paginated collection of Twitter profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.profile("@copperfield_lab_x_test").followers.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-followers-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/profiles/{identifier}/followers", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/followers", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.profile.followers.list, one request per page."""
        page: Page[models.TwitterProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterProfileFollowingResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterProfile]:
        """List profile following

        Return a cursor-paginated collection of Twitter profiles. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.profile("@copperfield_lab_x_test").following.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-following-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/profiles/{identifier}/following", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/following", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.profile.following.list, one request per page."""
        page: Page[models.TwitterProfile] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterProfileMediaResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """List profile media

        Return a cursor-paginated Twitter profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.profile("@copperfield_lab_x_test").media.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-media-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/profiles/{identifier}/media", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/media", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.profile.media.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterProfilePostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """List profile posts

        Return a stable page of posts for one public profile. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.profile("@copperfield_lab_x_test").posts.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-posts-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor, "since": since}
        result = self._transport.request("get", "/v1/twitter/profiles/{identifier}/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, since: str | datetime | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.profile.posts.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(freshness=freshness, cursor=cursor, since=since, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterProfileRepliesResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """List profile replies

        Return a cursor-paginated Twitter profile collection. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.profile("@copperfield_lab_x_test").replies.list(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-replies-list
        """
        params: dict[str, Any] = {"freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/profiles/{identifier}/replies", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/profiles/{identifier}/replies", self._bindings, params, timeout, max_retries)

    def items(self, *, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.profile.replies.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterProfileResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.followers = TwitterProfileFollowersResource(transport, bindings)
        self.following = TwitterProfileFollowingResource(transport, bindings)
        self.media = TwitterProfileMediaResource(transport, bindings)
        self.posts = TwitterProfilePostsResource(transport, bindings)
        self.replies = TwitterProfileRepliesResource(transport, bindings)

    def get(self, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.TwitterProfile]:
        """Get a profile

        Return one public profile. Use @username or a platform ID as the identifier. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.profile("@copperfield_lab_x_test").get(freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-profile-get
        """
        params: dict[str, Any] = {"freshness": freshness}
        result = self._transport.request("get", "/v1/twitter/profiles/{identifier}", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)


class TwitterSearchMediaResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, type: Literal["image", "video"], freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """Search Twitter media

        Search public Twitter media with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.search.media.list(q="synthetic", type="image", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-media-list
        """
        params: dict[str, Any] = {"q": q, "type": type, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/search/media", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/media", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, type: Literal["image", "video"], freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.search.media.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(q=q, type=type, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterSearchPostsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, sort: Literal["latest", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterPost]:
        """Search Twitter posts

        Search public Twitter posts with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.search.posts.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-posts-list
        """
        params: dict[str, Any] = {"q": q, "sort": sort, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/search/posts", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/posts", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, sort: Literal["latest", "top"] | None = None, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterPost]:
        """Iterate items lazily across pages of twitter.search.posts.list, one request per page."""
        page: Page[models.TwitterPost] | None = self.list(q=q, sort=sort, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterSearchSuggestionsResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterProfile]:
        """Search suggestions

        Search suggestions from public Twitter data. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.search.suggestions.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-suggestions-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/search/suggestions", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/suggestions", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.search.suggestions.list, one request per page."""
        page: Page[models.TwitterProfile] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterSearchUsersResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings

    def list(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Page[models.TwitterProfile]:
        """Search Twitter users

        Search public Twitter users with cursor pagination. Test and Live use the same route, parameters, response schema, pagination, and errors. In Test, null means unavailable, not zero.

        Example:
            openhandle.twitter.search.users.list(q="synthetic", freshness="24h")

        See https://openhandle.dev/docs/api-reference/twitter-search-users-list
        """
        params: dict[str, Any] = {"q": q, "freshness": freshness, "cursor": cursor}
        result = self._transport.request("get", "/v1/twitter/search/users", self._bindings, params, timeout=timeout, max_retries=max_retries)
        return self._transport.page(result, "get", "/v1/twitter/search/users", self._bindings, params, timeout, max_retries)

    def items(self, *, q: str, freshness: Freshness | None = None, cursor: str | None = None, timeout: float | None = None, max_retries: int | None = None) -> Iterator[models.TwitterProfile]:
        """Iterate items lazily across pages of twitter.search.users.list, one request per page."""
        page: Page[models.TwitterProfile] | None = self.list(q=q, freshness=freshness, cursor=cursor, timeout=timeout, max_retries=max_retries)
        while page is not None:
            yield from page.data
            page = page.next()


class TwitterSearchResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.media = TwitterSearchMediaResource(transport, bindings)
        self.posts = TwitterSearchPostsResource(transport, bindings)
        self.suggestions = TwitterSearchSuggestionsResource(transport, bindings)
        self.users = TwitterSearchUsersResource(transport, bindings)


class TwitterResource:
    def __init__(self, transport: SyncTransport, bindings: dict[str, str]) -> None:
        self._transport = transport
        self._bindings = bindings
        self.search = TwitterSearchResource(transport, bindings)

    def list(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TwitterListResource:
        """Select the list resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="twitter", resource="list")
        return TwitterListResource(self._transport, {**self._bindings, "identifier": identifier})

    def post(self, reference: str | None = None, *, id: str | None = None, url: str | None = None) -> TwitterPostResource:
        """Select the post resource using a supported string shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, id=id, url=url, platform="twitter", resource="post")
        return TwitterPostResource(self._transport, {**self._bindings, "identifier": identifier})

    def profile(self, reference: str | None = None, *, username: str | None = None, id: str | None = None, url: str | None = None) -> TwitterProfileResource:
        """Select the profile resource using a username shorthand or an explicit ID or URL reference.

        Resource selection is synchronous and does not perform a request.
        """
        identifier = resolve_selector(reference, username=username, id=id, url=url, platform="twitter", resource="profile")
        return TwitterProfileResource(self._transport, {**self._bindings, "identifier": identifier})


class GeneratedClient:
    def __init__(self, transport: SyncTransport) -> None:
        self._transport = transport
        self.instagram = InstagramResource(transport, {})
        self.reddit = RedditResource(transport, {})
        self.test_data = TestDataResource(transport, {})
        self.tiktok = TikTokResource(transport, {})
        self.twitter = TwitterResource(transport, {})

    def fetch(self, url: str, *, freshness: Freshness | None = None, timeout: float | None = None, max_retries: int | None = None) -> Response[models.FetchResource]:
        """Fetch the resource at a social URL

        Parse a supported social URL locally, then return the same typed resource as its canonical Openhandle operation. TikTok short links such as tiktok.com/t/… and vm.tiktok.com/… are expanded through their redirect first. The underlying data read is metered once.

        Example:
            openhandle.fetch("https://www.instagram.com/openai/", freshness="24h")
        """
        if not url or not url.strip():
            raise TypeError("fetch(url) requires a non-empty social URL.")
        body: dict[str, Any] = {"url": url}
        if freshness is not None:
            body["freshness"] = freshness
        result = self._transport.request("post", "/v1/urls/fetch", {}, {}, json_body=body, timeout=timeout, max_retries=max_retries)
        return self._transport.response(result)
